//state
const getDefaultState = () => {
	return {
		loaded: false,	
	}
}
const state = getDefaultState()
const mutations = {}
const getters = {}
const actions = {
  
}
export default {
	namespaced: true,
	state,
	mutations,
	getters,
	actions,
}